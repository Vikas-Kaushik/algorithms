#include <iostream

using namespace std;

int main() {
  int testCount;
  cin >> textCount;
  while(testCount-- != 0) {
    int n;
    cin >> n;
    int** arry = new int*[n];
    for(int i = 0; i < n; ++i)
      ary[i] = new int[n];
  }
}
