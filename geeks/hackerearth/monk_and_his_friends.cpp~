#include <iostream>
#include <vector>

using namespace std;

int main(){
  int testCount;
  cin >> testCount;

  while(testCount-- != 0) {
    int N, M;
    cin >> N;
    cin >> M;

    vector<int> listN;
    while(N-- != 0) {
      int i;
      cin >> i;
      listN.add(i); 
    }
    //sort list

    while(M-- != 0) {
      if(found) {
	cout << "YES" << endl;
      }
      else {
	cout << "NO" << endl;
      }
    }
  }
  return 0;
}

if(binary_search(vector.begin(), vector.end(), item)){
     // Find the item
}
